namespace KeraLua
{
    public enum LuaRegistry
    {
        /* LUAI_MAXSTACK		1000000 */
        Index = -1000000 - 1000
    }

    public enum LuaRegistryIndex
    {
        MainThread = 1,
        Globals = 2,
    }
}
